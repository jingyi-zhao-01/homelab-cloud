{{- if .Values.externalSecrets.enabled }}
{{- $chartValues := .Values }}
{{- $ssm := include "leetcode-intelligence.ssmParameters" . | fromYaml }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecrets.targetSecretName }}
  namespace: {{ include "leetcode-intelligence.namespace" . }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | quote }}
  secretStoreRef:
    kind: {{ .Values.externalSecrets.store.kind }}
    name: {{ .Values.externalSecrets.store.name }}
  target:
    name: {{ .Values.externalSecrets.targetSecretName }}
    creationPolicy: Owner
  data:
    {{- range $secretKey, $entry := $ssm }}
    {{- if or (ne $secretKey "API_KEY") $chartValues.externalSecrets.optionalKeys.apiKey }}
    - secretKey: {{ $secretKey }}
      remoteRef:
        key: {{ $entry.path | quote }}
        {{- if default false $entry.optional }}
        optional: true
        {{- end }}
    {{- end }}
{{- end }}
