namespaceOverride: ""

imagePullSecrets:
  - name: ghcr-pull-secret

image:
  repository: ghcr.io/jingyi-zhao-01/control-plane-triage-agent
  tag: latest
  pullPolicy: Always

deploymentMetadata:
  gitSha: ""

replicaCount: 1

resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    cpu: "1"
    memory: "1Gi"

persistence:
  enabled: true
  accessModes:
    - ReadWriteOnce
  size: 2Gi
  storageClassName: ""

nodeSelector: {}

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: node-role.kubernetes.io/control-plane
              operator: Exists

tolerations:
  - key: node-role.kubernetes.io/control-plane
    operator: Exists
    effect: NoSchedule
  - key: node-role.kubernetes.io/master
    operator: Exists
    effect: NoSchedule

env:
  POLL_INTERVAL_SECONDS: "120"
  LOOKBACK_MINUTES: "180"
  MAX_RUNS_PER_REPO: "20"
  MAX_LOG_BYTES: "524288"
  MAX_DISCORD_CHARS: "1800"
  STATE_DIR: "/var/lib/control-plane-triage-agent"
  INCIDENT_RETENTION_MAX_COUNT: "25"
  INCIDENT_RETENTION_MAX_AGE_DAYS: "7"
  OPERATOR_HISTORY_MAX_BYTES: "131072"
  OTEL_EXPORTER_OTLP_PROTOCOL: "http/protobuf"
  OPENHANDS_ENABLED: "true"
  OPENHANDS_MODEL: "openrouter/free"
  OPENHANDS_MAX_ITERATIONS: "30"
  WATCH_TARGETS_JSON: >-
    [
      {"repository":"jingyi-zhao-01/homelab-cloud","namespace":"flashsale","branch":"main"},
      {"repository":"jingyi-zhao-01/homelab-cloud","namespace":"strategy-tester","branch":"main"},
      {"repository":"jingyi-zhao-01/homelab-cloud","namespace":"leetcode-intelligence","branch":"main"}
    ]

secrets:
  existingSecretName: control-plane-triage-agent-secrets

externalSecrets:
  enabled: true
  refreshInterval: 1h
  targetSecretName: control-plane-triage-agent-secrets
  parameterMapFile: ssm-parameter-keys.yaml
  # OpenHands key is intentionally optional; keep enabled while source key
  # may be created later in a separate rotation/provision flow.
  optionalKeys:
    openhandsApiKey: true
  store:
    kind: ClusterSecretStore
    name: control-plane-triage-agent-aws-ssm
    create: true
    region: us-west-1
    authSecret:
      name: aws-ssm-credentials
      namespace: external-secrets
      accessKeyIdKey: access-key-id
      secretAccessKeyKey: secret-access-key
