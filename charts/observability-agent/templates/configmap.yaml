apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "observability-agent.fullname" . }}-script
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "observability-agent.labels" . | nindent 4 }}
data:
  observability_agent.py: |
{{ .Files.Get "files/observability_agent.py" | indent 4 }}
