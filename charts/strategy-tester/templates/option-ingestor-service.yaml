apiVersion: v1
kind: Service
metadata:
  name: {{ include "strategy-tester.fullname" . }}-option-ingestor
  namespace: {{ include "strategy-tester.namespace" . }}
  labels:
    app: option-ingestor
    {{- include "strategy-tester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.optionIngestor.service.type }}
  ports:
    - port: {{ .Values.optionIngestor.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: option-ingestor
    {{- include "strategy-tester.selectorLabels" . | nindent 4 }}
