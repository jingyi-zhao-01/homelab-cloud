{{- if .Values.snapshotIngestor.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "strategy-tester.fullname" . }}-snapshot-ingestor
  namespace: {{ include "strategy-tester.namespace" . }}
  labels:
    app: snapshot-ingestor
    {{- include "strategy-tester.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "strategy-tester.fullname" . }}-snapshot-ingestor
  minReplicas: {{ .Values.snapshotIngestor.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.snapshotIngestor.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.snapshotIngestor.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
