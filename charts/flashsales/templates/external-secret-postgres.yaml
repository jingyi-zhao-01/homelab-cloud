{{- if .Values.externalSecrets.enabled }}
apiVersion: {{ .Values.externalSecrets.apiVersion | quote }}
kind: ExternalSecret
metadata:
  name: flashsales-postgres-auth
  namespace: {{ include "flashsales.namespace" . }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | quote }}
  secretStoreRef:
    kind: {{ .Values.externalSecrets.store.kind }}
    name: {{ .Values.externalSecrets.store.name }}
  target:
    name: {{ .Values.externalSecrets.targetSecretName }}
    creationPolicy: Owner
  data:
    - secretKey: POSTGRES_USER
      remoteRef:
        key: {{ printf "%s/POSTGRES_USER" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: {{ printf "%s/POSTGRES_PASSWORD" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: POSTGRES_DB
      remoteRef:
        key: {{ printf "%s/POSTGRES_DB" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: DATABASE_URL
      remoteRef:
        key: {{ printf "%s/DATABASE_URL" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: {{ .Values.observability.tracing.existingSecretEndpointKey }}
      remoteRef:
        key: {{ .Values.observability.tracing.endpointSsmParameter | quote }}
    - secretKey: {{ .Values.observability.tracing.existingSecretKey }}
      remoteRef:
        key: {{ .Values.observability.tracing.headersSsmParameter | quote }}
    {{- if .Values.externalSecrets.includeUpstashRedis }}
    - secretKey: REDIS_URL
      remoteRef:
        key: {{ printf "%s/REDIS_URL" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: UPSTASH_REDIS_ENDPOINT
      remoteRef:
        key: {{ printf "%s/UPSTASH_REDIS_ENDPOINT" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: UPSTASH_REDIS_PORT
      remoteRef:
        key: {{ printf "%s/UPSTASH_REDIS_PORT" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: UPSTASH_REDIS_PASSWORD
      remoteRef:
        key: {{ printf "%s/UPSTASH_REDIS_PASSWORD" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: UPSTASH_REDIS_REST_TOKEN
      remoteRef:
        key: {{ printf "%s/UPSTASH_REDIS_REST_TOKEN" .Values.externalSecrets.parameterPrefix | quote }}
    - secretKey: UPSTASH_REDIS_READ_ONLY_REST_TOKEN
      remoteRef:
        key: {{ printf "%s/UPSTASH_REDIS_READ_ONLY_REST_TOKEN" .Values.externalSecrets.parameterPrefix | quote }}
    {{- end }}
{{- end }}
